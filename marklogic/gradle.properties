# Properties generated by mlNewProject at Sun Aug 30 09:28:58 EDT 2020
# See https://github.com/marklogic-community/ml-gradle/wiki/Property-reference for a list of all supported properties
mlAppName=tde-editor
mlHost=localhost
mlUsername=admin
mlPassword=admin
mlNoRestServer=true
mlModulePaths=src/main/ml-modules,src/test/ml-modules,../ui/tde-editor/build,../ui/tde-editor/public
# The gradle daemon keeps a file lock on files it works with, preventing the build process from replacing them
org.gradle.daemon=false